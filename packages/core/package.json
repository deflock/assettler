{
  "name": "@assettler/core",
  "version": "0.5.0",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=8.0"
  },
  "scripts": {
    "clean": "rimraf lib",
    "compile": "babel src --out-dir lib --root-mode upward",
    "recompile": "yarn clean && yarn compile",
    "pretest": "yarn recompile"
  },
  "dependencies": {
    "@deflock/aggregate-watcher": "^0.1.0",
    "@deflock/crypto": "^0.1.0",
    "@deflock/fs": "^0.1.0",
    "@deflock/path": "^0.1.0",
    "@deflock/string-trim": "^0.1.0",
    "anymatch": "^2.0.0",
    "fast-glob": "^2.2.6"
  }
}
